name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Tests with Coverage
        run: npm run test:cov

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 14

      - name: Coverage Summary (Job Summary)
        run: |
          echo "## 📊 Code Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('coverage/coverage-summary.json', 'utf8'));
            const t = data.total;
            const emoji = (pct) => pct >= 80 ? '✅' : pct >= 60 ? '⚠️' : '❌';
            const rows = [
              '| Metric | Coverage | Covered | Total |',
              '|--------|:--------:|--------:|------:|',
              \`| \${emoji(t.statements.pct)} Statements | \${t.statements.pct}% | \${t.statements.covered} | \${t.statements.total} |\`,
              \`| \${emoji(t.branches.pct)} Branches | \${t.branches.pct}% | \${t.branches.covered} | \${t.branches.total} |\`,
              \`| \${emoji(t.functions.pct)} Functions | \${t.functions.pct}% | \${t.functions.covered} | \${t.functions.total} |\`,
              \`| \${emoji(t.lines.pct)} Lines | \${t.lines.pct}% | \${t.lines.covered} | \${t.lines.total} |\`,
            ];
            rows.forEach(r => console.log(r));
          " >> $GITHUB_STEP_SUMMARY

      - name: Post Coverage Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const coverage = JSON.parse(fs.readFileSync('coverage/coverage-summary.json', 'utf8'));
            const t = coverage.total;
            const emoji = (pct) => pct >= 80 ? '✅' : pct >= 60 ? '⚠️' : '❌';
            const row = (name, d) =>
              `| ${emoji(d.pct)} **${name}** | ${d.pct}% | ${d.covered}/${d.total} |`;

            const marker = '<!-- coverage-report -->';
            const body = [
              marker,
              '## 📊 Code Coverage Report',
              '',
              '| Metric | Coverage | Covered / Total |',
              '|--------|:--------:|----------------:|',
              row('Statements', t.statements),
              row('Branches',   t.branches),
              row('Functions',  t.functions),
              row('Lines',      t.lines),
              '',
              '> ✅ ≥ 80%&nbsp;&nbsp;⚠️ 60–79%&nbsp;&nbsp;❌ < 60%',
              '',
              `_Updated: ${new Date().toISOString()}_`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(
              c => c.user.type === 'Bot' && c.body.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  badge:
    name: Update Coverage Badge
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Coverage Report
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: coverage/

      - name: Generate Coverage Badge SVG
        run: node scripts/generate-badge.js

      - name: Commit Badge to badges Branch
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create or update the badges branch via a git worktree
          git fetch origin badges:refs/remotes/origin/badges 2>/dev/null || true

          if git show-ref --verify --quiet refs/remotes/origin/badges; then
            git worktree add /tmp/badges-branch origin/badges
          else
            git worktree add --orphan -b badges /tmp/badges-branch
          fi

          cp .badges/coverage.svg /tmp/badges-branch/coverage.svg

          cd /tmp/badges-branch
          git add coverage.svg
          if git diff --cached --quiet; then
            echo "Badge unchanged – nothing to commit."
          else
            git commit -m "chore: update coverage badge [skip ci]"
            git push origin badges
          fi
